# Images Directory Security Protection
# Protects the main images directory from unauthorized access

# Allow image files
<FilesMatch "\.(jpg|jpeg|png|gif|webp|avif|svg)$">
    Require all granted
</FilesMatch>

# Deny everything else
<FilesMatch "^(?!.*\.(jpg|jpeg|png|gif|webp|avif|svg)$).*$">
    Require all denied
</FilesMatch>
