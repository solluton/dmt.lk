"use strict";

function glightboxGalleryInit() {
  GLightbox().on("open", function (o) {
    console.log("lightbox opened");
  }), GLightbox({
    selector: ".glightbox2"
  }), GLightbox({
    selector: ".glightbox3"
  }).on("slide_changed", function (_ref) {
    var o = _ref.prev,
      e = _ref.current;
    console.log("Prev slide", o), console.log("Current slide", e);
    o = e.player;
    o && (o.ready || o.on("ready", function (o) {}), o.on("play", function (o) {
      console.log("Started play");
    }), o.on("volumechange", function (o) {
      console.log("Volume change");
    }), o.on("ended", function (o) {
      console.log("Video ended");
    }));
  }), GLightbox({
    selector: ".glightbox4"
  });
}
glightboxGalleryInit();